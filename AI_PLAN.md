# flayAI ? ?? LLM + RAG ?? ??

? PC? LLM? RAG? ???? (1) ?? ??? ?????? (2) ?? ?? ??? ???? ????.
?? + ?? ??(REST ??)?? ?? ???? ????.

---

## 1. ?? & ????

### 1.1 PC ??
- CPU: i7-13700K (3.40 GHz)
- RAM: 32GB (?? 12~15GB ??)
- GPU: RTX 4070 Ti 12GB (?? 1~3GB)
- DISK: NVMe 2TB

### 1.2 ?? ????

**?? ??**
- ?? ???? ??? ??
- ???/???? ???????? ?? ("?? ??" ?? ??? ??)
- ?? ? ?? ?? ? ???
- ??/? ??
- ??? OCR ??
- JP/EN ? KO ??
- ?? ??(play/rank/likes) ?? ?? ("? ? ?", "??? ? ??? ?")
- **instance/archive ??** ?? (?? ? ? ?? ? vs ??? ??)

**?? ?? ??** (Phase 5)
- OneDrive ?? ??. ?? ????? ?.

### 1.3 ?? / ??
- TS/JS ??. ? ? ?? ??.
- **localhost ??** ?? (?? ?? ??).
- ?? ??? ??? OK.

---

## 2. ??? ??

### 2.1 ?? ?? (`K:\Crazy\Info\video.json`)
?? ?? ?? JSON. `opus`(??)? PK.
```json
{
  "opus": "BLK-628",
  "play": 1,
  "rank": 4,
  "lastPlay": 1689515599000,
  "lastAccess": 1756209604125,
  "lastModified": 1756209604132,
  "comment": "",
  "title": "???????????????",
  "desc": "??????????",
  "tags": [
    { "id": 17, "name": "????", "group": null, "description": "??, ??", "lastModified": 0 }
  ],
  "likes": [ 1689517988754 ]
}
```
> title/desc? ??? ???. tags ??? **id? ??**, name/desc? ???(`tag.json`) ??.

### 2.2 ?? (`K:\Crazy\Info\actress.json`)
```json
{
  "favorite": true,
  "name": "Aoi",
  "localName": "?",
  "otherNames": ["Ono Yuko"],
  "birth": "1994?12?03?",
  "body": "92H - 58 - 88",
  "height": 160,
  "debut": 2014,
  "comment": "Ono Yuko ? ??.",
  "lastModified": 1774533869840,
  "coverSize": 0
}
```
> ? **`otherNames`** ? ?? ?/? ??. **?? ??**? ?? ??. ??(birth/body/height ?) ? **`lastModified`? ? ?** ??.

### 2.3 ??? (`K:\Crazy\Info\studio.json`)
```json
{ "name": "Switch", "company": "", "homepage": null, "lastModified": 0 }
```

### 2.4 ?? (`K:\Crazy\Info\tag.json`, `tagGroup.json`)
```json
// tag
{ "id": 72, "name": "ASMR", "group": "screen", "description": "??, ??, ???", "lastModified": 1732940214000 }
// tagGroup
{ "id": "screen", "name": "????", "desc": "", "lastModified": -1 },
```

### 2.5 ???? (`K:\Crazy\Info\history.csv`)
```csv
2026-05-14 23:27:13, SSIS-560, PLAY, [S1][SSIS-560][??][Kawakita Saika][2022.10.25]
2026-05-14 23:31:30, SSIS-560, UPDATE, {"opus":"SSIS-560","play":8,...}
```

### 2.6 ??? ?? (~20,000?)
- ???: jpg / png / webp
- ??? ??? ?? (`config.yaml`? `poster_roots` ? ??? ??? ??)
- ??? ??: `[studio][opus][title][actressList][release].ext`
- ??: `K:\Crazy\Storage\kira\[kira][BLK-628][?? ??? ???][Kinoshita Himari, Mori Hinako][2023.07.18].jpg`
- **`actressList` (4?? `[]`) ? video?actress ??? ??? source of truth.** ?? ??? unknown, ?? ???? ?? ??.

### 2.7 ? Instance vs Archive
???? ??? ??/?? ??? ?? ???:

| ?? | ?? | ?? |
|------|------|------|
| **instance** | ???? **?? stem(??? ?? ??)** ? ?? ??(mp4/mkv/avi/wmv/mov/m4v ?)? ?? ??? ?? | ?? ??? ?? ?? |
| **archive** | `K:\Crazy\Archive\` ??? ?? (?? ??? ???) | ??????? ??, ?? ?? |

? ?? ??? `kind` (instance / archive) ??, ?? `kind=instance`/`archive`/? ? ??.

### 2.8 ?? ?? (Phase 5)
OneDrive ?? ??. ??? ??: txt, md, pdf, docx, xlsx, ??? ?.

---

## 3. ????

```
????????????????????     HTTP/SSE      ????????????????????
? ?? ?? / ?? ? ?????????????????? FastAPI          ?
? (Next.js ? UI)  ?                   ? Gateway          ?
????????????????????                   ????????????????????
                                                 ?
                                       ?????????????????????
                                       ? LlamaIndex Router ?
                                       ? (LLM Tool Calling)?
                                       ?????????????????????
                  ??????????????????????????????????????????????????????????????
                  ?              ?               ?              ?              ?
           ????????????   ????????????   ????????????   ????????????   ????????????
           ? Ollama   ?   ? Qdrant   ?   ? SQLite   ?   ? FAISS    ?   ? FAISS    ?
           ? LLM      ?   ? (text +  ?   ? (meta +  ?   ? posters  ?   ? faces    ?
           ?          ?   ?  OCR)    ?   ?  FTS5)   ?   ? (CLIP)   ?   ? (Insight)?
           ????????????   ????????????   ????????????   ????????????   ????????????
                  ?              ?              ?              ?              ?
                  ?????????????????? ??? ????? ????????????????????????????????
                                 (translate / OCR / embed / face)
```

**??**
- **?? ??**: ? ???? PC ??, 127.0.0.1 ???.
- **?? ??**: LLM / ??? / ?? / ?? / ??? ??.
- **????? ??**: SQL(??) + Vector(??) + CLIP(???) ? LLM ??.
- **??? ???**: ? ?? resumable, `state.json` ?????.

---

## 4. ?? & ?? ?? (Final)

### 4.1 ?? (RTX 4070 Ti 12GB)

| ?? | ?? | VRAM | ?? |
|------|------|------|------|
| LLM | `huihui_ai/qwen2.5-abliterated:14b` (Q4_K_M) | ~9GB | ??? ??, ?? ?? ??? |
| LLM ?? | `dolphin-2.9.4-llama3.1-8b` Q5 | ~6GB | ?? ??? ??? ? |
| ??? ??? | `BAAI/bge-m3` | ~2GB | ?/?/?, dense + sparse |
| ??? ??? | `OpenCLIP ViT-L/14` (laion2b_s32b_b82k) | ~2GB | ??? ??? GPU ?? |
| ?? ??/??? | `InsightFace (buffalo_l)` | ~1GB | ??? ? ?? ????? |
| OCR | `PaddleOCR` (?/?/? ??) | ~1.5GB | ?? ? `manga-ocr` ?? |
| ?? JP?KO | `Helsinki-NLP/opus-mt-ja-ko` | CPU OK | ?? ?? ? LLM ?? |

> NSFW: instruct ??? ??? ?? ?? ?? ? `abliterated`(refusal direction? weight?? ??) ??. M0?? NSFW ?? ???? 1~2?? ?? ??.

### 4.2 ?? ??

| ??? | ?? | ?? |
|--------|------|------|
| LLM ?? | **Ollama** | ?? ??, OpenAI ?? API |
| ??????? | **LlamaIndex** (Python) | RAG / tool calling / ???? |
| API | **FastAPI** (Python) | LlamaIndex ?? ???, OpenAPI ???? |
| ?? UI | **Next.js + TypeScript** | ??? ?? |
| ?? DB | **Qdrant** (Docker) | ?? ?? ??, ?? ?? ?? |
| ?? DB | **SQLite** + FTS5(trigram) | 24MB ??? ??, ?/? ???? |
| ??? ?? | **FAISS** (IndexFlatIP) | 20K ???? ?? |
| ??? | **uv** (Python), **pnpm** (Node) | ?? dependency ?? |
| ???? | **Docker Compose** | Qdrant ?? ?? |

> Python? ????: ???/OCR/CLIP/InsightFace ???? Python ??. UI? ??? TS.

---

## 5. ??? ??

### 5.1 SQLite ??? (`data/sqlite/flay.db`)

`opus`(??)? ?? ???? join key.

```sql
-- ??
CREATE TABLE videos (
  opus           TEXT PRIMARY KEY,
  title_jp       TEXT,
  title_ko       TEXT,           -- JP?KO ?? ??
  desc_jp        TEXT,
  desc_ko        TEXT,
  studio         TEXT,           -- ??? 1?? [] ?? video.json
  release_date   TEXT,           -- 'YYYY-MM-DD'
  release_year   INTEGER,
  release_month  INTEGER,
  comment        TEXT,
  play           INTEGER DEFAULT 0,
  rank           INTEGER DEFAULT 0,
  last_play      INTEGER,
  last_access    INTEGER,
  last_modified  INTEGER,
  like_count     INTEGER DEFAULT 0,
  has_poster     INTEGER DEFAULT 0,
  kind           TEXT             -- 'instance' | 'archive' | NULL
);
CREATE INDEX idx_videos_year_month ON videos(release_year, release_month);
CREATE INDEX idx_videos_rank       ON videos(rank DESC, last_play DESC);
CREATE INDEX idx_videos_kind       ON videos(kind);

-- FTS5 (???/??? ????, RRF ???)
CREATE VIRTUAL TABLE videos_fts USING fts5(
  opus UNINDEXED, title_jp, title_ko, desc_jp, desc_ko, comment,
  tokenize = 'trigram'
);

-- ?? (canonical ??; otherNames? ?? alias ????)
CREATE TABLE actresses (
  canonical_name TEXT PRIMARY KEY,   -- ?? ?? (?? ?)
  display_name   TEXT,                -- UI ?? (?? ?? lastModified? name)
  local_name     TEXT,                -- ??/?? ??
  favorite       INTEGER DEFAULT 0,
  birth          TEXT,
  body           TEXT,
  height         INTEGER,
  debut          INTEGER,
  comment        TEXT,
  last_modified  INTEGER,             -- ??? ??? ???
  cluster_id     INTEGER              -- InsightFace ??
);

-- ?? ?? (otherNames, localName, name ?? ?? ? canonical_name ??)
CREATE TABLE actress_aliases (
  alias_norm     TEXT PRIMARY KEY,    -- normalize_actress(alias)
  alias_raw      TEXT,
  canonical_name TEXT NOT NULL REFERENCES actresses(canonical_name)
);
CREATE INDEX idx_alias_canonical ON actress_aliases(canonical_name);

-- ??-?? N:M (??? actressList ??, alias ?? canonical? ??)
CREATE TABLE video_actresses (
  opus            TEXT,
  canonical_name  TEXT,
  PRIMARY KEY (opus, canonical_name)
);

-- ???
CREATE TABLE studios (
  name      TEXT PRIMARY KEY,
  company   TEXT,
  homepage  TEXT
);

-- ?? / ??
CREATE TABLE tag_groups (
  id    TEXT PRIMARY KEY,
  name  TEXT,
  desc  TEXT
);
CREATE TABLE tags (
  id          INTEGER PRIMARY KEY,
  name        TEXT,
  group_id    TEXT,
  description TEXT
);
CREATE TABLE video_tags (
  opus    TEXT,
  tag_id  INTEGER,
  PRIMARY KEY (opus, tag_id)
);

-- ??? ??? (video.likes)
CREATE TABLE likes (
  opus  TEXT,
  ts    INTEGER,
  PRIMARY KEY (opus, ts)
);

-- ???? (history.csv)
CREATE TABLE history (
  ts       INTEGER,
  opus     TEXT,
  action   TEXT,             -- PLAY / UPDATE / ...
  payload  TEXT,
  PRIMARY KEY (ts, opus, action)
);

-- ??? + instance/archive ??
CREATE TABLE posters (
  opus       TEXT PRIMARY KEY,
  path       TEXT NOT NULL,
  ext        TEXT,
  size       INTEGER,
  mtime      INTEGER,
  ocr_text   TEXT,
  kind       TEXT,             -- 'instance' | 'archive'
  video_path TEXT              -- instance? ?? ?? ?? ?? ??
);

-- ?? ???? ? ?? ??
CREATE TABLE face_clusters (
  cluster_id     INTEGER PRIMARY KEY,
  canonical_name TEXT,
  sample_count   INTEGER,
  confidence     REAL
);
CREATE TABLE poster_faces (
  poster_opus  TEXT,
  face_idx     INTEGER,
  cluster_id   INTEGER,
  bbox         TEXT,
  PRIMARY KEY (poster_opus, face_idx)
);

-- ?? ??
CREATE TABLE translations (
  hash      TEXT PRIMARY KEY,   -- sha1(text + model)
  src_lang  TEXT,
  tgt_lang  TEXT,
  src_text  TEXT,
  tgt_text  TEXT
);

-- ?? ??
CREATE TABLE query_log (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  ts          INTEGER,
  endpoint    TEXT,
  query       TEXT,
  tool_calls  TEXT,
  latency_ms  INTEGER,
  result_n    INTEGER,
  user_rating INTEGER
);
```

### 5.2 Qdrant ???
| ??? | ?? | payload ?? ?? |
|--------|------|-------------------|
| `videos` | bge-m3 (1024d) | opus, year, month, canonical_actresses[], tag_ids[], studio, rank, play, like_count, last_play, kind |
| `poster_ocr` | bge-m3 | opus, kind |
| `personal_docs` | bge-m3 | path, mtime, mime |

### 5.3 FAISS
| ??? | ?? | ?? |
|--------|------|------|
| `posters.index` (IndexFlatIP) | 768 (CLIP ViT-L) | id ? opus ? |
| `actor_faces.index` | 512 (InsightFace) | id ? (poster_opus, face_idx) |

### 5.4 ?? ?? ?? (otherNames)

```python
def build_actress_master(records: list[dict]) -> tuple[list[Actress], list[Alias]]:
    # 1. Union-Find: name + otherNames + localName ?? ???
    uf = UnionFind()
    for r in records:
        keys = [r["name"]] + r.get("otherNames", []) + ([r["localName"]] if r.get("localName") else [])
        keys = [normalize_actress(k) for k in keys if k]
        for k in keys[1:]:
            uf.union(keys[0], k)

    # 2. ? ??? ?? canonical ?? + ?? ??? lastModified ? ? ??
    groups = uf.groups()
    out_actresses, out_aliases = [], []
    for group_keys in groups:
        members = [r for r in records if normalize_actress(r["name"]) in group_keys]
        latest = max(members, key=lambda r: r.get("lastModified", 0))
        canonical = normalize_actress(latest["name"])

        merged = Actress(
            canonical_name=canonical,
            display_name=latest["name"],
            local_name=latest.get("localName"),
            favorite=any(m.get("favorite") for m in members),
            birth=latest.get("birth"),
            body=latest.get("body"),
            height=latest.get("height"),
            debut=latest.get("debut") or min((m.get("debut") for m in members if m.get("debut")), default=None),
            comment=latest.get("comment"),
            last_modified=latest.get("lastModified", 0),
        )
        out_actresses.append(merged)
        for k in group_keys:
            out_aliases.append(Alias(alias_norm=k, alias_raw=k, canonical_name=canonical))
    return out_actresses, out_aliases

def normalize_actress(name: str) -> str:
    import unicodedata, re
    n = unicodedata.normalize("NFKC", name).strip().lower()
    return re.sub(r"\s+", " ", n)
```

?? ??: actress.json ?? N? ? canonical M? (M ? N), `actress_aliases` ? ?? alias? ??? 1? canonical? ??.

### 5.5 ??? ??

```python
import re
PATTERN = re.compile(
    r'^\[(?P<studio>[^\]]+)\]'
    r'\[(?P<opus>[^\]]+)\]'
    r'\[(?P<title>.+)\]'                 # greedy
    r'\[(?P<actresses>[^\]]*)\]'
    r'\[(?P<release>\d{4}\.\d{2}\.\d{2})\]'
    r'\.(?P<ext>jpg|png|webp)$',
    re.IGNORECASE
)
# actresses: ',' split ? trim ? normalize ? alias ??? lookup ? canonical_name
# release:  '2023.07.18' ? '2023-07-18'
# ?? ?? ? unmatched_posters.log
```

### 5.6 Instance / Archive ??

```python
VIDEO_EXTS = {".mp4", ".mkv", ".avi", ".wmv", ".mov", ".m4v", ".ts", ".webm"}
ARCHIVE_ROOT = Path("K:/Crazy/Archive").resolve()

def classify_poster(poster_path: Path) -> tuple[str, Path | None]:
    if ARCHIVE_ROOT in poster_path.resolve().parents:
        return "archive", None
    stem = poster_path.stem  # ??? ?? ?? ??
    for ext in VIDEO_EXTS:
        cand = poster_path.with_suffix(ext)
        if cand.exists():
            return "instance", cand
    return "instance", None  # ??? ??? archive? ?? ? instance(???)? ??, kind=instance
```

> ??: archive ?? ???? ?? ??? ??? `kind="instance"`(?? ??)? ??, `video_path=NULL`? ??. ?? ? `playable=true` ???? `video_path IS NOT NULL` ??.

---

## 6. ??? ?????

### 6.1 ?? (resumable)

```
[1] load_jsons      studio.json, actress.json (??), tag*.json, video.json
                    ? studios, actresses, actress_aliases, tag_groups, tags,
                      videos, video_tags, likes
[2] scan_posters    config.poster_roots ?? + ??? ??
                    ? posters (kind ?? ??), video_actresses (canonical),
                      videos.has_poster=1, videos.kind ???
[3] history_csv     ??? ts ??? append
[4] translate       JP?KO (opus-mt-ja-ko, translations ??)
                    ? videos.title_ko, desc_ko, FTS ???
[5] embed_text      bge-m3 ? Qdrant `videos`
                    ?? = "[?? JP] {title_jp}\n[?? KO] {title_ko}\n
                            [??] {desc_ko}\n??: {canonical_actresses}\n
                            ??: {tag_names + descriptions}\n
                            ??: {studio}\n??: {YYYY-MM}\n
                            ???: {comment}\n??: {kind}"
[6] ocr_posters     PaddleOCR ? posters.ocr_text + Qdrant `poster_ocr`
[7] embed_clip      OpenCLIP ViT-L ? FAISS posters.index
[8] extract_faces   InsightFace ??/??? ? poster_faces
                    HDBSCAN ????? ? face_clusters
                    ?? ??: ???? ? video_actresses ?? ? canonical_name
[9] personal_docs   (Phase 5) OneDrive ?? ?? ? Qdrant `personal_docs`
```

### 6.2 ????? (`data/state.json`)
```json
{
  "schema_version": 1,
  "stages": {
    "load_jsons":    { "done": true,  "rows": 12345 },
    "scan_posters":  { "done": true,  "scanned": 19877, "matched": 19560, "unmatched": 317,
                       "instance": 18002, "archive": 1558 },
    "translate":     { "done": false, "cursor_opus": "SSIS-512", "completed": 8300 },
    "embed_text":    { "done": false, "completed": 0 },
    "ocr_posters":   { "done": false, "completed": 0 },
    "embed_clip":    { "done": false, "completed": 0 },
    "extract_faces": { "done": false, "completed": 0 },
    "history_csv":   { "last_ts": 1715712000000 }
  }
}
```
- 1000??? atomic write (tmp ? rename)
- SIGINT ? cursor ?? ? ??

### 6.3 ?? ?? ?? (4070 Ti)

| ?? | ??? | 20K? ?? |
|------|--------|-----------|
| JSON ETL + actress ?? | - | < 1? |
| ??? ?? + instance/archive ?? | ~30K files/min | ~1? |
| JP?KO ?? (opus-mt) | ~50 docs/sec (CPU) | ~7? |
| bge-m3 ??? | ~200 docs/sec (GPU) | ~2? |
| PaddleOCR | ~3 imgs/sec (GPU) | **~2??** |
| OpenCLIP ViT-L | ~30 imgs/sec (GPU) | ~12? |
| InsightFace + HDBSCAN | ~10 imgs/sec | ~40? |

**?? ? ???**: ? 3?? ? ?? 1?.
**?? (?? 100?)**: 5? ??.

### 6.4 VRAM ??? ??
- ??? ??: `ollama stop ...` ?? LLM unload ? CLIP/OCR ??
- ?? ???: `embed_text ? ocr ? embed_clip ? faces`
- ?? ??: LLM + bge-m3 ? ??, CLIP/InsightFace? lazy + idle 60s ? ??
- `/api/index/refresh`: LLM unload ? ??? ? reload

---

## 7. ?? / ??

### 7.1 ??? ??

| ??? ?? | ?? |
|-------------|------|
| "Aoi ???" | actress_aliases lookup ? canonical ? SQL JOIN |
| "? ???" / "Ono Yuko ???" | ?? canonical? ?? ? ?? ?? |
| "?? ?? ??" | bge-m3 Qdrant top-30 + FTS5 top-30 ? RRF ? ?? ?? ? top-10 |
| "?? ? ? ?? \~" | ? + payload `kind=instance AND video_path IS NOT NULL` ?? |
| "??? ?? ?? \~" | payload `kind=archive` ?? |
| ?? ??? ? ?? | InsightFace ? FAISS faces ? cluster_id ? canonical_name ? SQL |
| "??? ??? ???" | OpenCLIP ??? ??? ? FAISS posters |
| "???? '??'" | Qdrant `poster_ocr` ?? SQLite `posters.ocr_text LIKE` |
| "2023? 7?" | `WHERE release_year=2023 AND release_month=7 ORDER BY rank DESC` |
| "??? ? ??? ?" | likes ?? N? ? ? Qdrant ??? ?? ? ??? ?? |
| "? ? ??" | `WHERE play=0 AND rank>=3` + ???? ?? ??? ??? |

???: **LLM tool calling** 1?, ?? ? LlamaIndex `RouterQueryEngine` ??.

### 7.2 ?? ??

```
final_score = 0.70 * semantic_sim                  # bge-m3 cosine
            + 0.15 * fts_score (BM25 normalized)
            + 0.10 * usage_boost                   # log(1+play) + 0.5*rank/5 + 0.3*log(1+like_count)
            + 0.05 * recency_boost                 # exp(-?days/180), last_play ??
```
???? `config.yaml` ?? ??, ???(§10.3)?? ??.

### 7.3 LLM Tool Schema (LlamaIndex `FunctionTool`)

```python
search_videos(
    query: str,
    year: int | None = None,
    month: int | None = None,
    actress: str | None = None,         # alias ?? ???
    tag: str | None = None,
    kind: Literal["instance","archive","any"] = "any",
    playable: bool | None = None,        # video_path ?? ??
    watched: bool | None = None,
    min_rank: int | None = None,
    limit: int = 10,
) -> list[VideoHit]

search_by_actress_face(image_b64: str, top_k: int = 5) -> list[ActressMatch]
search_by_poster_text(query: str, top_k: int = 20) -> list[PosterHit]    # CLIP
search_poster_ocr(query: str, top_k: int = 10) -> list[VideoHit]
similar_to(opus: str, exclude_watched: bool = True) -> list[VideoHit]
translate(text: str, target: Literal["ko","en","ja"] = "ko") -> str
stats(actress: str | None, tag: str | None, year: int | None) -> dict
```

?? tool? **read-only**. write? ?? admin ???.

### 7.4 ?? ?? ??
- `opus-mt-ja-ko` 1?, ?? (sha1+model)
- ?? ??? ??? 30% ?? ?? 300% ?? ? LLM ??
- title? ???, desc? ?? ?? ?? ? ?? ? ??

---

## 8. API & UI

### 8.1 REST ?????

```
POST /api/chat                  # ?? ?? (??? + SSE ????)
POST /api/search/videos         # { query, filters }
POST /api/search/by-face        # multipart ???
POST /api/search/by-poster      # CLIP text/image ? ???
POST /api/ocr                   # ?? ??? OCR
POST /api/translate             # { text, target }
GET  /api/videos/:opus
GET  /api/actresses/:canonical  # alias ?? ??
GET  /static/posters/:opus      # ??? ?? ??
POST /api/index/refresh         # ?? ??? (admin)
GET  /api/admin/stats           # 24h ?? ??
GET  /api/face/clusters         # ??? ???? ??
POST /api/face/label            # cluster_id ? canonical_name ???
```
OpenAPI ??? `/docs` ?? ??, ?? ??? ??? ??.

### 8.2 ?? UI (Next.js, Phase 3)
- ?? ??? (SSE ????, abort ??)
- ?? ?? ??: ??? ??? + ?? + `kind` ??(instance/archive) + ??(???, ??? ?)
- ?? ??? ???: ??? ???? 9-grid + ?? ?? ????
- ??? LAN ?? ?? (127.0.0.1 only)

### 8.3 ?? ?? ??
- flayAI? ?? REST ???. ?? ??? HTTP? ??.
- CORS ??????: `http://127.0.0.1:3000` + ?? ?? origin (config? ??)
- SDK? ??? (?? ? OpenAPI?? ?? ??)

---

## 9. ?? / ??

### 9.1 ?? ?????
- [ ] FastAPI bind: `127.0.0.1` only. `0.0.0.0` ? startup abort
- [ ] Qdrant Docker port: `127.0.0.1:6333:6333`
- [ ] Ollama: `OLLAMA_HOST=127.0.0.1:11434`
- [ ] CORS ?????? ??
- [ ] `/api/admin/*` localhost ?? ??
- [ ] LLM tool? read-only, write? admin ???
- [ ] ??? ???: 10MB ??, ??? ??????
- [ ] K: ???? ???? ? fail-fast
- [ ] ?? ?? RAG? ?? Qdrant ??? + ??? ??

### 9.2 ?? (`scripts/backup.ps1`)
```powershell
$ts  = Get-Date -Format "yyyyMMdd-HHmmss"
$dst = "K:\Backup\flayAI\$ts"
New-Item -ItemType Directory -Path $dst -Force | Out-Null
sqlite3 data/sqlite/flay.db "VACUUM INTO '$dst/flay.db'"
curl -X POST http://127.0.0.1:6333/collections/videos/snapshots
curl -X POST http://127.0.0.1:6333/collections/poster_ocr/snapshots
curl -X POST http://127.0.0.1:6333/collections/personal_docs/snapshots
Copy-Item data/qdrant/snapshots/* "$dst/qdrant/" -Recurse
Copy-Item data/faiss/*.index "$dst/faiss/"
Copy-Item data/state.json,config.yaml "$dst/"
```
??: ?? 7? + ?? 1? (rotation).

### 9.3 ?? / ???
- `structlog` JSON ? `logs/flayai.log` (rotate 100MB × 10)
- SQLite `query_log` ???? ??/??/??/??? ?? ??
- `/api/admin/stats`: 24h ?? latency, ?? ??, ???

### 9.4 ?? ???
- `scripts/nightly_index.ps1` ? Windows Task Scheduler ??
- ??: LLM unload ? translate ? embed_text ? (??? ??? ? 1?) ? reload

---

## 10. ???? (M0 ~ M6)

? ????? **??? + ?? ??**. ??? ? ?? ?? ??.

### M0 ? ?? ?????
**???**: `scripts/bootstrap.ps1`, `docker-compose.yml`, `pyproject.toml`, `config.yaml`
**??**:
- `nvidia-smi` GPU ??
- `docker compose up -d qdrant` ? `curl http://127.0.0.1:6333/healthz` = 200
- `ollama pull huihui_ai/qwen2.5-abliterated:14b` ??
- ?? ???? 3? (??? ?? / ??????? ?? / ?? NSFW) ?? ??, ?? 0
- bge-m3 ??? smoke test ??

### M1 ? ????? ETL
**???**:
- `packages/indexer/load_jsons.py` (?? ?? ??)
- `packages/indexer/poster_parser.py`
- `packages/indexer/poster_scanner.py` (instance/archive ?? ??)
- `packages/indexer/history.py`
- `tests/test_poster_parser.py`, `test_actress_merge.py`

**??**:
- `flay-index load` ? ?? ??? row count > 0, FK ?? 0
- ?? ?? ??: `Aoi` ? `Ono Yuko` ? `?` ? ?? canonical? ??
- `flay-index scan` ? ??? ? 95%, instance/archive ??? ??
- `flay-index history` ? last_ts == CSV ??? ? ts
- pytest ??

### M2 ? ?? + ??? + ?? API (PoC)
**???**: `translate.py`, `embed_text.py`, `packages/rag/{retriever,ranker,router,tools}.py`, `apps/api/main.py` + routers
**??**:
- ?? 100? ?? ?? ??? ? 80%
- ?? ??? ? Qdrant `videos` ?? ?? ?? (top-1 cosine ? 0.5 ?? > 70%)
- `POST /api/chat` ?? 7? ???? ? 6/7:
  1. "Kawakita Saika ??? 5?"
  2. "Aoi ???" / "Ono Yuko ???" ?? ?? ??
  3. "?? ??? ?? ??"
  4. "2023? 7? ???"
  5. "S1 ??? ?? 4 ??"
  6. "?? ? ? ?? ?? ??" (instance?)
  7. "?? ??? ?? ? ??? ?"
- p95 latency ? 3? (???? ? ??)
- OpenAPI `/docs` ??

### M3 ? ?? UI
**???**: `apps/web/` (Next.js + Tailwind), ?? + SSE + ?? ?? (kind ?? ??)
**??**: M2 7? ????? UI?? ??, abort ??, LAN ?? ??

### M4 ? ??? ?? (CLIP + Face)
**???**: `embed_clip.py`, `faces.py`, `routers/face.py`, ?? ??? ???
**??**:
- ??? ??? 100%, FAISS top-1 self-search recall = 1.0
- ?? ???? ? ?? ?? ?? ??? ? 80% (?? 50? ??)
- ?? 1? ??? ? ?? top-5 ?? ? 2?

### M5 ? OCR + ?? ?? RAG
**???**:
- `ocr.py` (PaddleOCR ??)
- `personal_docs.py` (OneDrive ?? ????)

| ??? | ?? |
|--------|------|
| `.txt`, `.md` | ??? |
| `.pdf` | `pypdf` (???) ? ?? OCR (???) |
| `.docx` | `python-docx` |
| `.xlsx` | `openpyxl` (??? ????) |
| `.jpg/.png/.webp` | PaddleOCR |
| ? ? | skip + ?? |

**??**:
- 20K ??? OCR ??, ?? 100? ??? ? 85%
- OneDrive 1? ? ???, ??? ??? ? 5%
- "? ?? 12? ???" ? ?? ?? top-3
- ?? ??? ?? ?? ??? (??? ?? ??)

### M6 ? ?? ???
**???**: `eval/golden.yaml`(30?) + `eval/run_eval.py`, `scripts/backup.ps1`, `scripts/nightly_index.ps1`, structlog + query_log, README
**??**:
- ??? ? 85%
- ? ??? ?? ?? ? ?? ?? ?? (E2E ??)
- 7? ?? ?? ??? ?? ??

---

## 11. ?? / ???

### 11.1 ??? (`eval/golden.yaml`, 30?)

| ???? | ?? | ? |
|----------|------|----|
| ?? ???? | 4 | "Ai Asakura ??? 3? ??" |
| ?? ??/?? | 3 | "?", "Ono Yuko" ? ?? ?? ?? |
| ??? ??? | 4 | "?? ??? ??" |
| ??? ??? | 2 | "????" |
| ??? ?? | 4 | "? ?? ? ???" |
| ?? ?? | 3 | "2022? 10?" |
| ?? ?? | 3 | "? ? ? ?? ?? ?" |
| instance/archive | 2 | "?? ? ? ?? ~", "??? ? ~" |
| ??? OCR | 2 | ??? ? ??? ?? |
| ?? ?? | 3 | ??? ?? ?? ? top-1 ?? |

??? ??:
```yaml
- id: actress-alias-001
  query: "Ono Yuko ???"
  expect:
    same_result_as: actress-alias-002   # "? ???"
    min_results: 1
- id: kind-instance-001
  query: "?? ? ? ?? ?? ??"
  expect:
    all_must_have: { kind: instance, video_path_present: true }
```

### 11.2 ??? ????

| ??? | ?? | ?? |
|--------|------|------|
| abliterated ??? ?? ?? | ?? ??? | M0 ??, ?? ? ?? Qwen + ?? ?? ??? |
| FTS5 trigram ?? ??? | ??? recall? | bge-m3 sparse ??, RRF ??? ?? |
| OCR ??? ??? ?? | ??? ?? ?? | `manga-ocr` ?? |
| ?? ?? ?? ?? | ??? ?? | ??? ??? ???, ?? ??? UI |
| ?? ?? ???? (????) | ??? ?? | otherNames ??? ??? union, ?? ??? ?? ? |
| K: ???? ???? | ?? ?? | settings?? fail-fast |
| Qdrant ?? | ?? ?? | ?? snapshot, FTS5??? degrade ?? |
| OneDrive ??? ?? | ?? ?? | ??? + 24h ? unmatched ?? |
| ?? ?? ??? ?? | ?? ?? ?? | nightly cursor ?? ?? |
| LLM? SQL injection? ?? | ??? ?? | tool? read-only, ???? ?????? |
| Instance ?? ? ?? ?? ?? (?? stem) | ??? kind | ?? ??: ??? stem ???. ?? fuzzy ?? ?? ?? |

---

## 12. ???? ?? (?? ?? ??)

```
flayAI/
??? docker-compose.yml
??? config.yaml
??? pyproject.toml
??? .python-version                     # 3.11
??? README.md
??? apps/
?   ??? api/
?   ?   ??? main.py                     # FastAPI entry, 127.0.0.1 ??
?   ?   ??? deps.py                     # DI: db, qdrant, ollama
?   ?   ??? settings.py                 # config.yaml ??
?   ?   ??? schemas.py                  # pydantic
?   ?   ??? routers/
?   ?       ??? chat.py
?   ?       ??? search.py
?   ?       ??? face.py
?   ?       ??? ocr.py
?   ?       ??? translate.py
?   ?       ??? admin.py
?   ??? web/                            # Next.js (M3?? ??)
??? packages/
?   ??? indexer/
?   ?   ??? cli.py                      # `flay-index <stage|all>`
?   ?   ??? state.py                    # state.json atomic R/W
?   ?   ??? load_jsons.py
?   ?   ??? actress_merge.py            # otherNames ?? ??
?   ?   ??? poster_parser.py
?   ?   ??? poster_scanner.py           # instance/archive ??
?   ?   ??? translate.py
?   ?   ??? embed_text.py
?   ?   ??? ocr.py
?   ?   ??? embed_clip.py
?   ?   ??? faces.py
?   ?   ??? personal_docs.py            # Phase 5
?   ?   ??? history.py
?   ??? rag/
?       ??? router.py                   # LLM tool routing
?       ??? tools.py                    # search_videos ?
?       ??? retriever.py                # Qdrant + FTS5 RRF
?       ??? ranker.py                   # 7.2 ??
?       ??? prompts.py
??? data/
?   ??? sqlite/flay.db
?   ??? faiss/{posters,actor_faces}.index
?   ??? qdrant/                         # docker volume
?   ??? state.json
??? eval/
?   ??? golden.yaml
?   ??? run_eval.py
??? scripts/
?   ??? bootstrap.ps1
?   ??? backup.ps1
?   ??? nightly_index.ps1
??? tests/
    ??? test_poster_parser.py
    ??? test_actress_merge.py
    ??? test_classify_kind.py
    ??? test_ranker.py
    ??? test_router.py
```

---

## 13. config.yaml (??)

```yaml
data:
  info_dir: "K:/Crazy/Info"
  poster_roots:
    - "K:/Crazy/Storage/kira"
    # ?? ??? ???
  archive_root: "K:/Crazy/Archive"
  poster_extensions: [jpg, png, webp]
  video_extensions:  [mp4, mkv, avi, wmv, mov, m4v, ts, webm]

models:
  llm:        "huihui_ai/qwen2.5-abliterated:14b"
  embedding:  "BAAI/bge-m3"
  clip:       "ViT-L-14/laion2b_s32b_b82k"
  ocr:        "paddleocr"
  translator: "Helsinki-NLP/opus-mt-ja-ko"
  face:       "buffalo_l"

ranking:
  semantic_weight: 0.70
  fts_weight:      0.15
  usage_weight:    0.10
  recency_weight:  0.05
  recency_half_life_days: 180

server:
  host:     "127.0.0.1"
  api_port: 8000
  web_port: 3000
  qdrant:   "http://127.0.0.1:6333"
  ollama:   "http://127.0.0.1:11434"
  cors_origins:
    - "http://127.0.0.1:3000"
    # ?? ?? origin ??
```

---

## 14. Day 1 ?? ??

```powershell
cd c:\kamoru\Workspace\git\flayAI
git init
ni -ItemType Directory apps,packages,data,eval,scripts,tests -Force

# Python ??
winget install --id=astral-sh.uv -e
uv init --python 3.11
uv add fastapi "uvicorn[standard]" llama-index-core llama-index-llms-ollama `
       llama-index-embeddings-huggingface llama-index-vector-stores-qdrant `
       qdrant-client sentence-transformers transformers torch `
       paddlepaddle-gpu paddleocr open_clip_torch insightface onnxruntime-gpu `
       hdbscan sqlite-utils structlog pydantic-settings pyyaml watchdog `
       python-docx openpyxl pypdf

# ???
docker compose up -d qdrant
ollama pull huihui_ai/qwen2.5-abliterated:14b

# ??
ollama run huihui_ai/qwen2.5-abliterated:14b "???? ???? ? ?"
curl http://127.0.0.1:6333/healthz
nvidia-smi

# config.yaml ??? (poster_roots ?? ?? ??)
notepad config.yaml
```

? 5??? ???? **M1 (????? ETL)** ?? ???? ????.
